---
description: git commit без push
agent: sisyphus-junior
subtask: true
---

Сделай коммит. НЕ ПУШЬ — `git push` выполняю только я сам, никогда не запускай его.

## Префиксы коммитов

Выбери префикс по тому, где были изменения:

| Префикс     | Где менялось                                   |
| ----------- | ---------------------------------------------- |
| `ai:`       | OpenCode — `.opencode/*`                                           |
| `api:`      | Бэкенд — `apps/api/*`                                              |
| `web:`      | Фронтенд — `apps/web/*`                                            |
| `composable:` | Vue-композаблы — `packages/composables/*`                       |
| `lib:`      | Библиотеки — `libs/*`                                              |
| `config:`   | Конфигурация — `shared/config/*`                                   |
| `schemes:`  | Схемы и фикстуры — `shared/schemes/*`, `shared/mocks/*`            |
| `stores:`   | Pinia-сторы — `shared/stores/*`                                    |
| `shared:`   | HTTP-клиент — `shared/api/*`                                       |
| `ui:`       | UI-кит — `packages/ui/*`                                           |
| `utils:`    | Утилиты — `packages/utils/*`                                       |
| `test:`     | Тестовая инфраструктура — `packages/test/*`                        |
| `style:`    | Правила стиля — `.rules`                                           |
| `wip:`      | Черновой коммит, будет переписан                                    |
| `ignore:`   | Вспомогательные изменения, не влияющие на сборку                    |

Если изменения затронули несколько областей — сделай отдельный коммит на каждую область. Не смешивай `api:` и `web:` в одном коммите.

## Сообщение коммита

- **Все сообщения коммитов — только на русском языке.** Никакого английского в subject и body.
- Объясняй ПОЧЕМУ изменение сделано, а не ЧТО поменялось
- Описывай с точки зрения разработчика проекта: как это влияет на код, архитектуру или DX
- Будь конкретным: «api: валидация постов через Effect Schema» вместо «api: улучшена работа с постами»
- Не пиши сообщения длиннее 72 символов в первой строке. Детали — в теле коммита

## Конфликты

При конфликтах — НЕ ИСПРАВЛЯЙ. Сообщи мне, я разберусь сам.

## GIT DIFF

!`git diff`

## GIT DIFF --cached

!`git diff --cached`

## GIT STATUS --short

!`git status --short`
